Please fix three specific issues identified from phone console logs.
Fix 1 — Notification tap registering as snooze: The phone console shows notificationclick action is snooze when the user taps the notification body. This is wrong — tapping the notification body should open the app not snooze. Please fix the notificationclick handler in sw.js. The handler currently has action buttons including snooze and the default action is being incorrectly identified as snooze. Fix the logic so that when event.action is empty string or undefined or equals open the handler opens the app at the Winston URL. Only treat the action as snooze if event.action explicitly equals the string snooze. The fix should be: if (!event.action || event.action === '' || event.action === 'open') then open the app. Add console log SW: opening app from notification tap when this path is taken.
Fix 2 — Phone SSE dropping with ERR_QUIC_PROTOCOL_ERROR: The phone SSE connection is dropping with a QUIC protocol error which is different from the 5 minute proxy timeout. This is a mobile Chrome network issue. Please add a specific error handler for the SSE EventSource that detects this error and immediately reconnects. When the SSE onerror fires log SSE ERROR: plus the error type then immediately close the existing EventSource and create a new one after a 1 second delay. Also add the header X-Accel-Buffering: no to the SSE endpoint response to prevent QUIC buffering issues.
Fix 3 — Phone not receiving SSE reminder event to trigger verbal alert: The phone receives push notifications but is not receiving the SSE reminder event that triggers fireReminderAlert and speaks the reminder. This is because when the reminder fires the SSE connection on the phone may be momentarily disconnected. The polling fallback at GET /api/reminders/due should catch this but may not be working correctly on mobile. Please verify the polling fallback runs every 30 seconds on mobile and correctly calls fireReminderAlert when it finds a fired reminder. Add console log POLL: found fired reminder plus the reminder text when the polling fallback catches a missed reminder.