Please fix three critical reminder system bugs completely. I need you to rebuild the reminder firing system from scratch with proper state management.
Bug 1 — Reminders not firing as push notifications on mobile: When a reminder time arrives the server scheduler should send a push notification to ALL registered push subscriptions for that user stored in Supabase. Please check that push subscriptions from the mobile device are being saved to Supabase correctly when the user registers for notifications on their phone. Add a Supabase table called push_subscriptions if it does not exist with columns for user_id, subscription_json, device_type, and created_at. Every device that grants notification permission should have its subscription saved to this table. When a reminder fires query this table for all subscriptions belonging to that user and send the push notification to every one of them.
Bug 2 — App on phone not triggering reminder in app: When a reminder is due and Winston is open on the phone Emma Peel should speak the reminder immediately without requiring a notification tap. Implement a polling system where the frontend checks for due reminders every 30 seconds by calling a GET endpoint at /api/reminders/due which returns any reminders that are due for the current user and have not been spoken yet. When the frontend receives a due reminder it should trigger Emma Peel to speak it immediately in her voice and then call a POST endpoint at /api/reminders/spoken to mark it as spoken so it does not fire again.
Bug 3 — Reminder repeating on laptop: This is happening because the reminder is not being marked as completed after it fires. The scheduler fires the reminder but does not update its status in Supabase. Please add a status field to the reminders table with values pending, fired, and completed. When a reminder fires change its status from pending to fired immediately before sending the push notification. After the push notification is sent successfully change the status to completed. The scheduler should only query reminders with status pending — never fired or completed. This prevents any reminder from firing more than once.
Additional fix — make sure reminder times are stored in UTC in Supabase but the scheduler converts to Central Time America/Chicago for comparison. When David says remind me at 3pm Emma Peel should interpret that as 3pm Central Time and store the equivalent UTC time in Supabase.
Please also add a GET endpoint at /api/reminders/list that returns all pending reminders for the current user so the frontend can display them. And add a DELETE endpoint at /api/reminders/delete that lets David cancel a reminder by ID