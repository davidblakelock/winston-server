Please fix all four reminder bugs in one comprehensive update based on the diagnosis you just provided.
Fix 1 — Single creation path: Create one shared function called createReminder that contains the duplicate guard checking for existing pending rows with same user plus text plus fire_at, the database insert, and the SSE broadcast of reminder_sync with action created. Replace the direct INSERT in chat.ts lines 1392 to 1433 with a call to this shared createReminder function. The API route in reminders.ts should also use this same shared function. This eliminates duplicate reminders and ensures the panel always updates immediately without page refresh.
Fix 2 — Panel real time updates: Since chat.ts will now use the shared createReminder function that broadcasts reminder_sync the panel will update immediately. Verify the SSE handler in Chat.tsx lines 1113 to 1138 correctly handles the created action by adding the new reminder to the panel immediately.
Fix 3 — Overdue display: In the panel do not show reminders as overdue. If a reminder's fire_at is in the past and its status is still pending either hide it completely or show it with a Past due label and an acknowledge button. Never show a reminder as overdue without giving the user a way to dismiss it.
Fix 4 — Recurring reminder reappear: After the scheduler resets a recurring reminder's fire_at to the next occurrence and sets status back to pending immediately broadcast reminder_sync with action created and include the updated fire_at so the panel can display the next occurrence correctly without requiring a page refresh.
After making all fixes please trace through this exact scenario to verify everything works. User says remind me to have a beer at 9pm on the laptop. Reminder appears immediately in the panel on both laptop and phone without page refresh. At 9pm push notification fires on both devices. Reminder disappears from panel on both devices. No duplicate. No overdue label. Correct companion name in notification