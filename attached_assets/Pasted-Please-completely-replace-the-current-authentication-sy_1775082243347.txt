Please completely replace the current authentication system with Google OAuth as the sole sign in method. Remove all email and magic link authentication code completely. Here is exactly how the new authentication should work. Step 1 — User opens Winston and sees the welcome screen with two buttons — Sign in with Google and See what Winston can do for demo. Step 2 — User clicks Sign in with Google and is redirected to Google's OAuth consent screen. Step 3 — User authenticates with their Google account. Step 4 — Google returns the user's unique Google ID, email address, name, and profile picture. Step 5 — Winston immediately clears any previous session data completely. Step 6 — Winston checks Supabase for an existing profile matching this Google ID. Step 7 — If a profile exists load it completely including companion name, voice preference, all saved data, conversation memory, reminders, lists, places, stories, and all other profile data. Take the user directly to the main chat interface. Emma Peel greets them warmly by name — Welcome back David. Good to have you here. Step 8 — If no profile exists for this Google ID start the onboarding conversation immediately. Step 9 — After onboarding completes save the complete new profile to Supabase with the Google ID as the unique identifier. Step 10 — Store the session token linked to this specific Google ID in local storage so the user stays signed in across sessions. Step 11 — Display the user's Google profile picture as a circular avatar in the top right corner alongside their companion name. Step 12 — When the user signs out completely clear all session data and local storage immediately and return to the welcome screen. The Google OAuth credentials are already stored in Replit secrets as GOOGLE_CLIENT_ID and GOOGLE_CLIENT_SECRET. The authorized redirect URI is already configured in Google Cloud Console. Use the existing Google OAuth implementation already in the codebase as the foundation — just make it the sole authentication method and ensure the profile loading and session management work correctly for both new and returning users on any device