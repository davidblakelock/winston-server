Please fix two critical notification issues completely.
Issue 1 — Notification actions not showing correctly: The push notifications are showing only an Unsubscribe option instead of custom actions. This means the service worker is not handling the push event correctly and the browser is falling back to default notification display. Please fix the service worker push event handler to display rich notifications with custom action buttons. The notification should show three action buttons: action 1 with action ID open and title Open Winston, action 2 with action ID dismiss and title Dismiss. The notification should use the full Winston URL https://winston-companion--davidblakelock.replit.app/ in the data payload. The notificationclick handler must handle both the main notification tap and each action button tap. If action is open or if the main notification body is tapped call clients.openWindow with https://winston-companion--davidblakelock.replit.app/. If action is dismiss just close the notification. Make sure the service worker is completely replacing the browser default notification handler.
Issue 2 — Reminders not syncing between devices: When David adds a reminder on his laptop it should immediately be available on his phone and any other device. Please fix the reminder storage system so that all reminders are stored in Supabase against David's Google user ID — not in local memory, local storage, or session storage. The node-cron scheduler on the server should check Supabase every minute for pending reminders for all users and send push notifications to all registered devices for that user. This means if David has both his laptop and his Pixel registered for push notifications both devices should receive the reminder notification. Fix the reminder creation flow so that when Emma Peel saves a reminder it always goes to Supabase immediately with the correct user ID. Fix the scheduler to query Supabase for pending reminders rather than checking any local or in-memory store