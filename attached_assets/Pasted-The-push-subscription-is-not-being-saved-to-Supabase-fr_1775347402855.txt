The push subscription is not being saved to Supabase from mobile devices. The push_subscriptions table only has an entry for the laptop — the Pixel has never successfully registered. Please completely rebuild the push notification subscription registration from scratch with these exact requirements.
On every page load run this registration flow automatically. First check if the browser supports service workers and push manager. Second check if notification permission is already granted. Third if permission is granted call pushManager.getSubscription to get any existing subscription. Fourth if a subscription exists send it immediately to POST /api/push/subscribe with the user's Google ID and a device identifier based on the user agent string. Fifth if no subscription exists call pushManager.subscribe with the VAPID public key to create one then send it to the same endpoint.
If notification permission is not yet granted show a prominent friendly prompt asking the user to enable notifications. When they tap allow go through the same flow.
On the backend the POST /api/push/subscribe endpoint must do these things. Receive the subscription object and user Google ID. Check if this exact subscription endpoint already exists in push_subscriptions for this user. If it exists update it. If it does not exist insert a new row. Always return a clear success or error response — never let this fail silently.
Add extremely detailed console logging at every single step on both frontend and backend. Log whether serviceWorker is supported, whether pushManager is supported, the permission state, whether getSubscription returned null or an existing subscription, whether subscribe succeeded or failed with the exact error, whether the POST to save succeeded or failed with the exact server response.
Also add a red bell icon in the header that shows when notifications are not registered for this device. Tapping it manually triggers the entire registration flow. Turn it green when a subscription is successfully saved to Supabase.
This registration must work on mobile Chrome on Android. Test specifically for Android Chrome compatibility.