Please make these four specific targeted fixes. Do not rewrite anything else — only change exactly what is described.
Fix 1 — Remove hardcoded Emma Peel from service worker: In sw.js line 1 change the comment from Emma Peel to Winston AI Companion. More importantly find everywhere in sw.js that the notification title or body uses a hardcoded name like Emma Peel and replace it with the companion name from the push payload data. The push payload sent by the scheduler should include the companion name — make sure scheduler.ts includes companion_name in the push notification payload by reading it from the user's profile before sending. Then sw.js should use payload.data.companion_name as the notification title.
Fix 2 — Verify SSE reminder event reaches Chat.tsx: In scheduler.ts line 81 the broadcast sends a reminder event with id, userName, reminderText, and speakText. In Chat.tsx the SSE useEffect around lines 1119 to 1189 listens for this event. Please add a console.log immediately when the reminder SSE event is received in Chat.tsx so we can confirm it is arriving. Also add a console.log inside fireReminderAlert when it is called so we can confirm that function executes. Do not change the logic — just add these two log lines.
Fix 3 — SSE reconnection on mobile: The exponential backoff reconnect is good but on mobile Chrome the SSE connection drops when the screen locks or the browser goes to background. When the SSE reconnects after a drop immediately call GET /api/reminders/due to check for any reminders that fired while disconnected. This ensures no reminder is missed when the phone wakes up.
Fix 4 — Verify push notification payload includes companion name: In scheduler.ts around lines 91 to 105 where sendPushToAll is called check that the notification payload includes the companion name from the user profile. The profile query should happen before building the push payload. If it is not already included add companion_name to the push data object so the service worker can use it as the notification title.
After making only these four targeted changes please tell me exactly what you changed in each file so I can verify nothing else was touched.