Please fix two remaining notification issues completely.
Issue 1 — Tapping Open Winston notification action does nothing on mobile. The problem is that notification action buttons use a different click handler than regular notification taps. Please fix the service worker notificationclick handler to correctly handle action buttons on mobile Chrome. The handler must check event.action — if it equals open or if event.action is empty meaning the main body was tapped do this: call event.notification.close first, then inside event.waitUntil use clients.matchAll with type window and includeUncontrolled true, if any client URL contains winston-companion--davidblakelock.replit.app call client.focus on it AND call client.postMessage with the notification data so the app knows which reminder fired, if no client is found call clients.openWindow with the exact URL https://winston-companion--davidblakelock.replit.app/ and store the notification data in IndexedDB so the app can retrieve it when it opens. This is critical — clients.openWindow only works inside event.waitUntil and must use the complete https URL.
Issue 2 — Winston app is not speaking the reminder when notification fires. The app needs to listen for two things. First a message from the service worker via navigator.serviceWorker.addEventListener message — when this fires with reminder data Emma Peel should immediately speak the reminder text in her voice. Second when the app opens after being launched from a notification it should check IndexedDB for any pending reminder data and if found immediately have Emma Peel speak it. The speaking should happen automatically without David having to type anything — Emma Peel proactively says something like Hey David your reminder to call Olivia is here.
The complete flow must work like this: reminder time arrives, server sends push notification, notification appears on phone with Open Winston button, David taps Open Winston, Winston opens or focuses, service worker sends message to app with reminder context, app receives message and Emma Peel immediately speaks the reminder in Bex's voice.
Please also add a fallback — if the app is already open when a reminder fires Emma Peel should speak the reminder immediately without requiring any notification tap at all.