Please fix three specific bugs.
Bug 1 — Google Calendar move not executing: When David asks to move a calendar event Winston confirms it was moved but the event is not actually being moved in Google Calendar. Please add detailed logging to the calendar update function. Log CALENDAR UPDATE: attempting to update event id plus the event ID. Log CALENDAR UPDATE: calling Google Calendar API patch endpoint. Log CALENDAR UPDATE: API response status plus the status code. Log CALENDAR UPDATE: success or CALENDAR UPDATE: FAILED plus the exact error. The most likely cause is that the Google Calendar events.patch call is not being made at all or the OAuth token does not have write permission. Please verify the OAuth scope includes https://www.googleapis.com/auth/calendar.events and not just readonly. If the scope is readonly that is why updates are silently failing — the API accepts the call but does not execute it. Fix the OAuth scope to include full calendar write access and prompt David to reconnect his Google Calendar to grant the new permission.
Bug 2 — Rule 1 voice broadcasting: User initiated conversations are still speaking on all devices. The speak_on_all logic is not correctly identifying user initiated versus system initiated messages. Please fix this with a simpler and more reliable approach. Add a field called initiated_by to every SSE message broadcast. When a user sends a message and James Bond responds set initiated_by to the device_id of the device that sent the message. On the frontend when a speak_sync event arrives check if initiated_by matches the current device_id. If it matches speak the response. If it does not match display the text via CHAT SYNC but do not call speakReply. For system initiated messages like reminders morning briefing evening wind down and proactive check-ins set initiated_by to system which causes all devices to speak. This is a clean reliable implementation that correctly handles both cases.
Bug 3 — Phone app catching up when opened: When David is using Winston on his laptop and the phone app is in the background or on the home screen the phone should not automatically open or speak. This is correct behavior. However when David opens the phone app it must immediately catch up and show the complete current conversation including all messages exchanged on the laptop. Please verify the visibility change handler is correctly fetching the latest chat messages from the server when the app comes to foreground. The fetch should retrieve all messages from the current session and update the chat display immediately. Add a console log VISIBILITY: app came to foreground fetching latest messages when this triggers so we can verify it is working