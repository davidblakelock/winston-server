Please fix the following six bugs completely.
Bug 1 — Emma Peel in evening wind down notifications: The evening wind down push notification and the wind down initiation message are still using the hardcoded name Emma Peel instead of the user's companion name. Search the entire codebase for every hardcoded reference to Emma Peel or Emma in notification payloads, wind down triggers, and scheduled messages. Replace every instance with a dynamic lookup of companion_name from user_profiles in Supabase before sending. No hardcoded companion names anywhere in the codebase.
Bug 2 — Evening wind down not working: The evening wind down is not initiating at the scheduled time. Please check the wind down scheduler and verify it is running correctly. The wind down should trigger at the user's preferred time — default 9pm Central Time — by sending an SSE winddown-start event to connected clients and a push notification to all registered devices. Add console logging to the wind down scheduler showing WINDDOWN: checking at plus current time and WINDDOWN: triggering for user plus username when it fires. Also verify the winddown_settings table has the correct time stored for David.
Bug 3 — Push notification tap not opening app on phone: When a push notification arrives on the phone tapping it does nothing or does not open Winston. Please completely rewrite the notificationclick handler in sw.js. It must do exactly this. Call event.notification.close first. Then inside event.waitUntil call self.clients.matchAll with type window and includeUncontrolled true. If any existing client URL contains winston-companion--davidblakelock.replit.app call client.focus on it and then call client.postMessage with the notification data. If no existing client is found call self.clients.openWindow with the exact URL https://winston-companion--davidblakelock.replit.app/ This openWindow call must be inside event.waitUntil or it will be blocked by the browser. Add console logging in the service worker showing SW: notification clicked and SW: opening window or SW: focusing existing window.
Bug 4 — App not responding in morning after overnight: Winston stops responding after being left open overnight on mobile. This is caused by the SSE connection dropping and not recovering, and the OAuth token expiring after one hour without refresh. Please fix both issues. First ensure the SSE keepalive ping is sending every 30 seconds with a flush call to prevent proxy timeout. Second implement automatic OAuth token refresh — before every API call check if the token expires within 5 minutes and refresh it silently using the refresh token stored in the session. Third when the SSE connection drops implement immediate reconnection with a maximum of 5 retries using exponential backoff starting at 1 second. Fourth on SSE reconnection immediately call GET /api/reminders/due with cache no-store to catch any missed reminders. Add a visible reconnecting indicator in the UI when the connection is lost so the user knows Winston is trying to reconnect.
Bug 5 — Morning briefing email summary inconsistent with manual email check: The morning briefing fetches a limited number of emails using one code path while asking James Bond to check emails uses a different code path that fetches the latest 10. Please unify these into one shared function called fetchAndSummarizeEmails that both the morning briefing and the manual email check use. This function should fetch the 15 most recent emails regardless of read status sorted by date descending, filter out newsletters and promotional emails automatically keeping only emails that appear personally addressed or require action, summarize the relevant ones concisely, and flag anything suspicious as a potential scam. Both the morning briefing and manual email check should call this exact same function so the behavior is identical.
Bug 6 — No Gmail and Calendar connect button when already logged in: When the user opens Winston and is already logged in there is no way to connect Gmail or Google Calendar without logging out and back in. Please add a Connect Gmail and Connect Calendar button to the settings panel that is always visible regardless of login state. If Gmail is already connected show a green Connected label with a Disconnect option. If not connected show a Connect Gmail button that triggers the OAuth flow without requiring logout. Same for Google Calendar. This should be accessible from the settings gear icon at any time